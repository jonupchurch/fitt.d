Architected comprehensive documentation bundle with ADRs, prompts, and evaluation frameworkArchitected comprehensive documentation bundle with ADRs, prompts, and evaluation frameworkLet's do the full bundle — these all reinforce each other, so I'll build them as one coherent repo scaffold you can drop in. This is documentation and supporting artifacts (the app code itself is for your Spec Kit / Claude in Code session); everything here is tailored to Fitt.d and the decisions we've already made.Diagnosed directory structure issues and devised cleanup strategyDiagnosed directory structure issues and devised cleanup strategyBrace expansion isn't supported by the shell here (it's dash), so the directories were created with literal names and the ADR files landed in the wrong place. Let me find the strays, clean up, and rebuild robustly with explicit paths.Devised comprehensive cleanup and reconstruction strategyDevised comprehensive cleanup and reconstruction strategyFound it — the shell starts at /, so the failed cd dropped those files at the filesystem root. Let me clean up and rebuild everything with explicit absolute paths.Orchestrated comprehensive file reconstruction with structured path organizationOrchestrated comprehensive file reconstruction with structured path organizationClean now. Rewriting all files with absolute paths.Orchestrated directory verification, packaging, and selective file extractionOrchestrated directory verification, packaging, and selective file extractionEverything's written. Let me verify the full tree and package it.Ran a command, read a fileRan a command, read a fileDone. The zip is the full scaffold (28 files) ready to unzip at your repo root; I also pulled out the README, a sample ADR, and the microcopy for quick preview without unzipping.
Here's what's inside and why each piece earns its place:
ADRs (docs/adr/) — an index, a template, and six written decisions: recording ADRs, Zod validation + repair-retry, ephemeral/no-persistence, the streaming-vs-blocking split, provider abstraction, and no-auth-for-MVP. These are the core evidence for your architect claim. Each one names the alternatives it rejected, which is the part that actually reads as architectural thinking rather than just description.
Prompts (prompts/) — four versioned prompt files (JD analysis, resume parse, gap analysis, bullet tailoring) with a README explaining the versioning discipline. Notice they're written to refuse to fabricate — the tailoring prompt explicitly can't invent experience. That honesty constraint is itself a product/ethics decision worth mentioning in an interview.
Evals (evals/) — a real harness (run-evals.ts + scorers.ts) that scores pipeline output against fixtures on schema validity, required-skill recall, no-hallucinated-matches, and score plausibility, and exits non-zero below threshold so it's a genuine CI gate. Two realistic fixtures are included. This is the rarest signal in the whole bundle — almost no portfolio has evals, and AI teams screen hard for exactly this.
Privacy + non-functional (docs/) — the in-memory/no-storage stance (doubles as a landing-page selling point) and explicit budgets for latency, cost, limits, and accessibility.
Microcopy (content/microcopy.md) — every UI state written in-voice, so nothing ships as lorem ipsum. The error copy explains-and-fixes instead of apologizing, per the design principles.
README — structured as the tour that bridges "nice site" → "read my repo," plus .env.example and an MIT license.
One thing I intentionally left as stubs: run-evals.ts has a TODO where the real pipeline import goes, because that code gets generated in your Spec Kit session — the harness, scorers, and fixtures are complete and waiting for it. Everything references the decisions we already made (Fitt.d, the brand tokens, Zod, streaming, ephemeral), so it should slot in with your existing spec, wireframes, and style guide without contradiction.
A natural sequencing note: these artifacts assume the architecture in your Spec Kit plan.md, so when you run the plan step in Claude in Code, point it at the docs/adr/ folder as input — that way the generated code and the committed decisions stay in agreement rather than drifting.